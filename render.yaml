services:
    type: web
    name: Student's_analysis_Abatsong_ange_merveilles_24G2727
    env: python 
    plan: free 
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
       key: PORT
       value: "10000" 