services:
    type: web
    name: tp-inf-232-analyse-des-donnees
    env: python 
    plan: free 
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
       key: PORT
       value: "10000" 